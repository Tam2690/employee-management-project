import styled from "styled-components";

const NoDataNotificationStyled = styled.div`
  position: absolute;
  left: 50%;
  transform: translateX(-50%);

  & img {
    width: 200px;
  }
`;

export default NoDataNotificationStyled;
